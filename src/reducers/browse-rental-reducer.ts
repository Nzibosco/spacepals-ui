import { IBrowseRentalsState } from ".";


const initialState: IBrowseRentalsState = {
    availableRentals:[]
}

export const rentalReducer = (state = initialState, action:any) => {
    switch (action.type) {
        case : {
            return {

            }
        }
        case : {
            return {

            }
        }
        default:
            return state

    }
}